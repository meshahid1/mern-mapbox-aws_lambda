import {describe, expect, test} from '@jest/globals'
import App from '../src/App';

describe('App', () => {
  describe('getPins', () => {
    
      test('Validate response', async () => {});
  })
});